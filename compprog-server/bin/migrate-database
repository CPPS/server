var migration = require('../service/migration');

migration.migrate([
    migration.model(require('../model/User')),

    migration.model(require('../model/Contest')),

    migration.model(require('../model/Provider')),

    migration.model(require('../model/Group'))
]);
