var migration = require('../utils/migration');

migration.migrate([
    migration.model(require('../app/model/User')),

    migration.model(require('../app/model/Contest')),

    migration.model(require('../app/model/Provider')),

    migration.model(require('../app/model/Group'))
]);
